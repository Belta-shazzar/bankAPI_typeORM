export enum TransactionType {
  WITHDRAW = "WITHDRAW",
  DEPOSIT = "DEPOSIT",
  TRANSFER = "TRANSFER",
}

export enum TransactionStatus {
    SUCCESS = "SUCCESSFUL",
    DECLINE = "DECLINED",
    PENDING = "PENDING"
}
